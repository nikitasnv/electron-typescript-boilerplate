import React from 'react';
import { hot } from "react-hot-loader/root";

export default hot((): React.ClassicElement<void> =>
    (
        <div >
            <h1 >
                Electron React App
            </h1 >
        </div >
    ));